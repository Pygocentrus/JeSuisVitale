/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var BlogRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return BlogRouter;
});
